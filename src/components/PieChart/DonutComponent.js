import {pieChartData} from '../../testData';
import React, {useState, useEffect} from 'react';

const DonutComponent = ({classes}) => {
    
    const [selectedPie, setSelectedPie] = useState(pieChartData);

    useEffect(() => {
        
    });

    return(
        <div>

        </div>
    )

}

